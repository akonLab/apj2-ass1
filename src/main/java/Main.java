public class Main {
    public static void main(String[] args) {
        //Search search = new Search();
        //search.start();

        //Search pdfSearch = new Search();
        // pdfSearch.readTxt(new Item(new File("src/main/documents/article_1.txt"))," a".trim());
/*
        ExecutorService executor = Executors.newCachedThreadPool();
        int count=1;
        for (File file : Objects.requireNonNull(new File("src/main/filesForTask1").listFiles())) {
            executor.execute(new Rename(file,count));
            count++;
        }
        */
    }
}
